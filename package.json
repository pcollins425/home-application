{
  "name": "home-application",
  "private": true,
  "version": "0.1.0",
  "description": "Personal home hub bridge — Plaid transaction dashboard (Cloudflare Worker + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply home_plaid --local",
    "db:migrate:remote": "wrangler d1 migrations apply home_plaid --remote",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260710.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.111.0"
  }
}
